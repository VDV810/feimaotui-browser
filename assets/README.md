# 图标资源

本项目需要以下图标文件：

- `icon.png` - 应用图标 (512x512 或 256x256)
- `icon.ico` - Windows 图标 (256x256)
- `icon.icns` - macOS 图标
- `tray-icon.png` - 托盘图标 (16x16 或 32x32)

你可以使用在线工具将 PNG 转换为 ICO 和 ICNS 格式。

打包前请确保这些图标文件存在，否则打包会失败。
