v2.9.1: 标记稳定序号(删除留空不复用) + 恢复上个标记(回收站可连续恢复)

- 每条标记永久序号seq: 列表显示圆圈序号,按seq排序;删除5后显示1,2,3,4,6,7,序号5留空不复用,方便按序号逐条排查异常标记
- 回收站: 删除/清空的规则进 custom-ad-rules-deleted.json(保留最近30条,重启不丢)
- 新增「恢复上个标记」按钮: 按删除时间倒序依次恢复,可连续点;恢复时若页面已有相同规则则跳过;恢复后自动刷新生效
- 老数据无seq自动补齐(按现有顺序),导出文件不带seq,对方导入自动重新编号
