v1.3.92: 标记规则作用域可控 - 收紧误伤 + 每条规则可一键切"仅本站/全站生效"

- 收紧v1.3.91: 无唯一类名链时不再用短类名链兜底(会命中整站同类元素误伤),回退旧式绝对路径只精确命中当前页面;唯一类名链仍跨账户/页面生效
- 每条规则新增作用域按钮: 仅xx站(灰) <-> 全站生效(绿),一键切换即时生效;切全站时原域名存originDomain可切回
- 删除/清空/导入/切换均无感更新样式表,不整页刷新
